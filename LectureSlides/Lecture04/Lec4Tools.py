import Lec4Stortage

# Parameters / Arguments (tree, rocks, sands)
def build_a_door(tree, rocks, sands):
    num_tree = take_tree_from_storage()
    if storage_tree - num_tree > need_tree:
        store_tree(storage_tree - num_tree)
    ...

def build_a_framework(tree, rocks, sands):
    ...